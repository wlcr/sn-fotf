/**
 * Root Sanity configuration file for CLI operations and codegen.
 * This references the studio configuration to maintain consistency.
 */

// Re-export the studio configuration for CLI tools
export {default} from './studio/sanity.config';
